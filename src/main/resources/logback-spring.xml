<?xml version="1.0" encoding="UTF-8"?>
<!--
  Default profile: human-readable console log, same as Spring Boot's default.
  Production profile: structured JSON via logstash-logback-encoder so Loki
    / Datadog / Promtail can scrape stdout directly.

  Activate JSON output by setting SPRING_PROFILES_ACTIVE=production (the
  existing prod env templates already do this).
-->
<configuration>
  <include resource="org/springframework/boot/logging/logback/defaults.xml" />

  <springProfile name="!production">
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
      <encoder>
        <pattern>${CONSOLE_LOG_PATTERN}</pattern>
        <charset>utf8</charset>
      </encoder>
    </appender>
    <root level="INFO">
      <appender-ref ref="CONSOLE" />
    </root>
  </springProfile>

  <springProfile name="production">
    <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
      <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
        <providers>
          <timestamp>
            <fieldName>ts</fieldName>
            <pattern>yyyy-MM-dd'T'HH:mm:ss.SSSXXX</pattern>
          </timestamp>
          <version>
            <fieldName>schema</fieldName>
          </version>
          <logLevel>
            <fieldName>level</fieldName>
          </logLevel>
          <loggerName>
            <fieldName>logger</fieldName>
            <shortenedLoggerNameLength>40</shortenedLoggerNameLength>
          </loggerName>
          <threadName>
            <fieldName>thread</fieldName>
          </threadName>
          <mdc />
          <message>
            <fieldName>msg</fieldName>
          </message>
          <stackTrace>
            <fieldName>stack</fieldName>
            <throwableConverter class="net.logstash.logback.stacktrace.ShortenedThrowableConverter">
              <maxDepthPerThrowable>30</maxDepthPerThrowable>
              <maxLength>4096</maxLength>
              <rootCauseFirst>true</rootCauseFirst>
            </throwableConverter>
          </stackTrace>
          <pattern>
            <pattern>{"app":"dam-kemon","env":"${SENTRY_ENV:-prod}"}</pattern>
          </pattern>
        </providers>
      </encoder>
    </appender>
    <root level="INFO">
      <appender-ref ref="JSON" />
    </root>
  </springProfile>
</configuration>
